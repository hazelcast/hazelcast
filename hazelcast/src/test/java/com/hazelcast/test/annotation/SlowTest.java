package com.hazelcast.test.annotation;

/**
 * A slot test is a test that executes reasonably fast, e.g. a few minutes. But takes too much time to be part
 * of the regular build.
 */
public class SlowTest {
}
