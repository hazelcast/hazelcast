hazelcast-jet:
  instance:
    cooperative-thread-count: ${thread.count}
    flow-control-period: ${flow.control.period}
    backup-count: ${backup.count}
    scale-up-delay-millis: ${scale.up.delay.millis}
    lossless-restart-enabled: ${lossless.restart.enabled}
  properties:
    property1: value1
    property2: value2
