hazelcast:
  cluster-name: jet
  network:
    join:
      multicast:
        enabled: false
      tcp-ip:
        interface: 127.0.0.1
        enabled: true
        member-list:
          - 127.0.0.1:5701
